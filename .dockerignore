# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments - don't mount host venv into container
.venv/
.env/
venv/
ENV/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log

# Temporary files
*.tmp
*.temp

# Git
.git/
.gitignore

# Documentation build
site/
docs/_build/

# Cache
.cache/
.mypy_cache/
.ruff_cache/
# The database and stored files are mounted, never baked: an image that
# carries a copy of app.db is an image a container can write into and lose.
data/
storage_data/

# Deploy rollback snapshots. `aegis deploy` writes these into the deploy
# directory on the host, which IS the build context there. Without this
# entry `COPY . /code` bakes every retained snapshot into the image, and
# since the copied bytes change on each deploy the layer is rebuilt every
# time. Left unignored on aegis-pulse this reached 2.5 GB of a 3.65 GB
# image and helped fill a 77 GB droplet.
backups/
